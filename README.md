## Live scraping exercise

A simple example of fetching a web page, parsing HTML and writing the results to a CSV file. Designed to be accessed through the browser with [binder](https://mybinder.org/).